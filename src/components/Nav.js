import { Link } from "react-router-dom";
import { BsInbox } from "react-icons/bs";

const Nav = () => {
  return (
    <nav>
      <div>
        <BsInbox />
      </div>
    </nav>
  );
};

export default Nav;
